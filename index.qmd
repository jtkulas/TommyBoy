---
title: "Address → Map"
format: html
runtime: shiny
execute:
  echo: false
---

```{r setup, include=FALSE}
# Hide code & messages in rendered output
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)

# Minimal libraries for this simplified app
library(shiny)
library(leaflet)

# Geocoder helper (keeps implementation separate)
source("R/helpers.R")
```

